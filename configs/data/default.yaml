batch_size: 64 
workers: 8
pin_memory: True
seq_comb: "4seq" # input sequence combination (4seq includes T1, T1C, T2, FLAIR)
aug_type: "heavy"

slice_percentile: 75 # input axial slice configuration
training_size: 224 # input image size

k_fold: 5 # number of folds in cross validation
fold_num: 0 # fold number

class_balance_load: False
